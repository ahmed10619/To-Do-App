import 'package:flutter/material.dart';

class OnBoradingScreens extends StatelessWidget {
  const OnBoradingScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
